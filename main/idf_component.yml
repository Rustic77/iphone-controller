## IDF Component Manager Manifest File
dependencies:
  espressif/esp_tinyusb:
    version: "^2.0.1~1"
  # Outbound WebSocket client for the cloud control relay (see cloud_client.c).
  espressif/esp_websocket_client:
    version: "^1.2.0"
